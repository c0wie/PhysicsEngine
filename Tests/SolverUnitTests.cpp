#include <gtest/gtest.h>
#include "Solver.hpp"

namespace
{
    TEST(Solver, positionSolverCollisionObject)
    {
        std::vector<pe2d::Collision> collisions = 
        {
            pe2d::Collision()
        };
    }
}